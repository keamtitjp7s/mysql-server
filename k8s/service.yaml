apiVersion: v1
kind: Service
metadata:
  name: mysql-server-service
  labels:
    app: mysql-server
spec:
  type: ClusterIP
  selector:
    app: mysql-server
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
